const Defaults = {
  attribute: "class" as "class" | `data-${string}`,
  darkTheme: "dark",
  defaultTheme: "light",
  lightTheme: "light",
  mediaQuery: true,
  setAttribute: true,
  storageKey: "theme",
  themes: ["light", "dark"],
};

export default Defaults;
